{
  "type": "breaking",
  "category": "services",
  "description": "Renamed `album_id` and `track_id` parameters to `id`, and `album_ids` and `track_ids` to `ids` in `AlbumService` and `TrackService` methods for consistency with `ArtistService`. ([#26](https://github.com/jonathan343/spotify-sdk/issues/26))"
}
